# Preferences

## CLI / Shell
- Always use `pip3` not `pip` for Python package installs
- Always use `--break-system-packages` flag with pip3 on this machine (Python 3.14 system install)
- Uses `python3` / `python -m` from ~/Developments/rebon

## Communication style
- Be concise and direct — no unnecessary explanation or verbosity
- No bullet points in conversational replies; prose only
- No emojis unless asked
